{
  "name": "@sda-chms/server",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./client": {
      "default": "./src/client.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b && tsc -p tsconfig.test.json",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js",
    "test": "vitest run",
    "import-people": "bun run ./scripts/import-people.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "@sda-chms/db": "workspace:*",
    "@sda-chms/env": "workspace:*",
    "@sda-chms/shared": "workspace:*",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "udsv": "^0.7.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@electric-sql/pglite": "^0.5.3",
    "typescript": "catalog:",
    "tsdown": "^0.22.3",
    "@sda-chms/config": "workspace:*"
  }
}
